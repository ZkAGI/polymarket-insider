/**
 * Polymarket Insider/Whale Tracker
 * Main entry point
 */

export const APP_NAME = "Polymarket Tracker";
export const VERSION = "1.0.0";

export function greet(name: string): string {
  return `Welcome to ${APP_NAME}, ${name}!`;
}
